import React, { Fragment } from "react";

import classes from "./ModalFooter.module.scss";

const ModalFooter = () => {
  return (
    <Fragment>
      <footer className={classes["modal__footer"]}></footer>
    </Fragment>
  );
};

export default ModalFooter;
